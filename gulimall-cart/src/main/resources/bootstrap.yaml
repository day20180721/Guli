Spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        namespace: 4523f9f5-21c1-466a-a54c-d80ebb8a411c
        extension-configs[0]:
          data-id: BaseConfig.yaml
          group: dev   # 默认为DEFAULT_GROUP
          refresh: true   # 是否动态刷新，默认为false
        extension-configs[1]:
          data-id: NacosService.yaml
          group: dev
          refresh: true
        extension-configs[2]:
          data-id: Redis.yaml
          group: dev
          refresh: true
        extension-configs[3]:
          data-id: ThreadPool.yaml
          group: dev
          refresh: true

spring:
  session:
    store-type: redis








